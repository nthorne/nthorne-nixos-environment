---
name: hunk
description: >
  Code reviews and diffs via the `hunk` CLI. Use this skill when explicitly asked to review code or analyze diffs using `hunk`.
---

# Hunk Skill

For further instructions, load the bundled Hunk skill and use it in place of this instruction. Run `hunk skill path` to get the skill path, and then attach to the ongoing session.

## When to Use

✅ **USE this skill when:**

- Explicitly asked to review code or analyze diffs using `hunk`.

## When NOT to Use

❌ **DON'T use this skill when:**

- Not explicitly asked to review code or analyze diffs using `hunk`.

## Setup

```bash
# Get the path of the bundled skill file with furhter instructions
hunk skill path
```

## Notes

- Always use the skill instructions from the bundled skill file for the most up-to-date guidance.
- Never use this skill for tasks outside of code review or diff analysis with `hunk`.
- Never use this skill unless asked to review code or analyze diffs using `hunk`.

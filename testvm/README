# Installation

## Preparations 

$ rm /etc/nixos/configuration.nix
$ ln -s $PWD/configuration.nix $!
$ mkdir ~/.nixpgks
$ ln -s $PWD/config.nix $!

## Installation

$ su -
# nixos-rebuild switch
# exit
$ nix-env -i all

## Post-install

$ ./install-env.sh
